# Tus datos reales van aquí — nunca se commitean, ni en tu propio fork.
# Solo templates/ (vacío, sin datos) se versiona. Anclado a la raíz (/) para
# no atrapar por accidente templates/profile/, que sí debe versionarse.
/profile/
/applications/
/cvs/
/cvs-pdf/
/config.yaml

# Notas de desarrollo de este proyecto — no son del usuario que hace fork.
# Local a propósito: la continuidad de groundwork no le sirve a quien lo usa.
/.claude/ESTADO.md

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# Editor
.vscode/
.idea/
